<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>StoreName</title>

    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://unicons.iconscout.com/release/v4.0.0/css/line.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
    <link rel="stylesheet" href="css/style.css" />
    <link rel="stylesheet" href="css/allbooks.css" />
</head>

<body>

    <%- include('header.ejs') %>

    <div class="radio-input">
        <label>
        <input type="radio" id="AllBooks" name="value-radio" value="value-1">
        <span>All Books</span>
        </label>
        <label>
        <input type="radio" id="fantasy" name="value-radio" value="value-2">
        <span>Fantasy</span>
        </label>
        <label>
          <input type="radio" id="horror" name="value-radio" value="value-3">
        <span>Horror</span>
        </label>
        <label>
          <input type="radio" id="fiction" name="value-radio" value="value-4">
        <span>Fiction</span>
        </label>
        <label>
            <input type="radio" id="romance" name="value-radio" value="value-5">
        <span>Romance</span>
        </label>
        <span class="selection"></span>
      </div>

    <div class="cat-name"><%= section %></div>
    <div class="container">
        <div class="row">
            <% allbooks.forEach((book, index) => { %>
                <% if (index % 5 === 0 && index !== 0) { %>
                    </div><div class="row">
                <% } %>
                <div class="col-md-2">
                    <a href="/bookpage/<%= encodeURIComponent(book.Book_Name) %>">
                        <img class="bookimg img-fluid" src="<%= book.Image %>" alt="<%= book.Book_Name %>">
                    </a><br>
                    <p><%= book.Book_Name %></p>
                </div>
            <% }); %>
        </div>

        <nav aria-label="Page navigation">
            <ul class="pagination">
                <% for (let i = 1; i <= totalPages; i++) { %>
                    <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                        <a class="page-link" href="?section=<%= section %>&page=<%= i %>"><%= i %></a>
                    </li>
                <% } %>
            </ul>
        </nav>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="js/script.js"></script>
</body>
</html>